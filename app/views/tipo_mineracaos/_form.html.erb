<%= form_with(model: tipo_mineracao, local: true) do |form| %>
  <% if tipo_mineracao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_mineracao.errors.count, "error") %> prohibited this tipo_mineracao from being saved:</h2>

      <ul>
      <% tipo_mineracao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>Nome</p>
  <div class="ui input focus">
    <%= form.text_field :nome %>
  </div>
  <br></br>

  <p>Sigla</p>
  <div class="ui input focus">
    <%= form.text_field :sigla %>
  </div>

  <br></br>

  <div class="actions">
    <%= form.submit %>
  </div>
  <br></br>
<% end %>


